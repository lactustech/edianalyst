# Cloudflare Workers (Static Assets). The site is a Next.js static export
# (output: 'export' → ./out); an assets-only Worker (no `main`) just serves those
# files. `npx wrangler deploy` uploads ./out and deploys.
name = "edianalyst"
compatibility_date = "2025-06-01"

[assets]
directory = "./out"
